export default function AdminCategories() {
  return (
    <div className="note">
      <div style={{ fontWeight: 900, marginBottom: 10 }}>Categories (demo UI)</div>
      <div className="muted small">
        Category CRUD is UI-only in this demo. The backend focuses on OOP processing + I/O endpoints.
      </div>
    </div>
  );
}
