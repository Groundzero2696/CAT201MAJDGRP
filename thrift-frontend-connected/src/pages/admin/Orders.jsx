export default function AdminOrders() {
  return (
    <div className="note">
      <div style={{ fontWeight: 900, marginBottom: 10 }}>Orders (demo UI)</div>
      <div className="muted small">
        Real customer orders are stored by the Java backend in data/orders.txt and visible in Account → Orders.
      </div>
    </div>
  );
}
